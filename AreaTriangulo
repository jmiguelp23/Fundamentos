#include <iostream>

using namespace std;

int main()
{
    float base, altura, area;
    cout<<"inserta base: ";
    cin>>base;
    cout<<"inserta altura: ";
    cin>>altura;
    area=(base*altura)/2;
    cout<<"el area es: "<<area;
    return 0;
}
